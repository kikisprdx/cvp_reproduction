[project]
name = "cvp-reproduction"
version = "0.1.0"
description = ""
authors = [{ name = "Kikis", email = "kikispcmr@gmail.com" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["datasets (>=4.8.5,<5.0.0)"]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
